# What and why

<!-- What changes, and what problem it solves. If there is an issue, link it: Closes #123 -->

## How it was verified

<!-- Not "tests pass" — what did you actually run or click?
     e.g. "Ran the app, created a service through the admin form, confirmed it
     persisted via GET /api/services." -->

- [ ] Ran locally (`docker compose up` or `npm run dev`)
- [ ] `npm run lint` and `npm run format:check` are clean
- [ ] Tests pass (`npm test`)

## Anything reviewers should look at closely

<!-- Trade-offs you made, things you were unsure about, follow-ups you left out. -->
